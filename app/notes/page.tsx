/*Серверний компонен експортує компонент NotesPage, який рендерить клієнтський компонент Notes.client.tsx.Використовується для відображення списку всіх нотаток.*/

import Notes from './Notes.client';

export default function NotesPage() {
  return <Notes />;
}
